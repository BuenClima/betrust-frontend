import { Grid } from '@mui/material'

/**
 * @description TipDetails component
 * @returns {JSX.Element} TipDetails component
 */
export const TipDetails = (): JSX.Element => {
  return <Grid container>TipDetails</Grid>
}

export default TipDetails
