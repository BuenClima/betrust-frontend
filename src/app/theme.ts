import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2191D0',
      contrastText: '#fff'
    },
    secondary: {
      main: '#ffffff',
      contrastText: '#000000'
    }
  }
})
